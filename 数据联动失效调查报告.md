# 数据联动失效调查报告

## 核心结论

改版将设计稿的 **6 页压缩为 3 tab**（饮食/训练/更多），跨域联动展示大面积丢失，Flexible Plan Engine 从未实现。

## 1. 今日总览页(page_05)联动：设计有，代码先实现后删除

设计稿 `page_05_today.py` 规划了跨饮食+训练的联动总览：热量环(目标/实际/消耗)、BURN消耗卡、DEFICIT赤字卡、**TRAINING今日训练横条**(部位+动作+预计kcal)、调整提示("明日计划已根据今日多摄入调整")。

代码历史(`97c1be34f7b40f4d@v1`)证实饮食页**曾经实现了训练横条**（第302行`{/* ===== 今日训练横条 ===== */}`，含`TRAINING / 今日训练`标题+动作列表+消耗badge）。工作日志明确记录**"饮食页移除训练横条"**，当前版本已删除该区块。仪表盘保留了消耗/赤字数字，但训练详情横条和"训练日可适当上调碳水"提示均已丢失。

## 2. 六页压缩为两tab时丢失的联动

| 设计页 | 联动内容 | 当前状态 |
|---|---|---|
| page_05 今日总览 | 饮食+训练+赤字+调整提示同页 | 训练横条已删除，拆分为独立tab |
| page_06 营养计划 | "碳水将根据今日训练量动态浮动" | **丢失**，无训练→碳水联动文案 |
| page_07 实际营养 | "分摊未来3天"+"保存并重排未来3天"按钮 | **丢失**，无重排按钮、无分摊预览 |
| page_08 训练计划 | "训练日碳水保留"配套饮食提示 | **丢失**，训练页无饮食提示 |
| page_09 实际训练 | "面拉顺延至明日"+"保存并重排训练队列" | **丢失**，train.tsx无调整卡、无重排按钮 |
| page_10 动态调整 | **独立调整页**：偏差来源+3原则+未来3天预览+训练队列调整+安全提示 | **整页缺失**，仅饮食页有小调整卡 |

## 3. 旧版web-preview对比

web-preview HTML 为当前代码导出，确认：
- `index.html`：有"今日消耗""热量赤字"，但**无**"重排/分摊/顺延/后续调整/动态调整"关键词
- `train.html`：副标题写"系统顺延未完成项并联动饮食"（仅文字），**无**任何实际调整展示、无赤字、无重排按钮

## 4. PRD联动机制未实现项

PRD第7节"Flexible Plan Engine"规划三类余额（热量/营养/训练）+未来3-7天重排+调整原因+风险提示。技术方案指定路径`shared/domain/plan-engine/`——**该目录为空**。

实际仅`features/adjustments.ts`中一个`buildDailyAdjustmentSummary`函数做基础热量delta计算，缺少：营养余额、训练余额、多日重排预览、训练队列顺延逻辑、安全边界提示。

PRD验收标准第10条"系统解释为什么这样调整"——设计稿page_10专门承载此功能，代码中**完全缺失**该独立页面。
