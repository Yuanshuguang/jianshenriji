# 2026-06-26 工作日志

## 仪表盘 MacroRow 布局调整
- 文件：`apps/mobile/app/(tabs)/index.tsx` 第 540-543 行
- 改动：MacroRow 组件中名称(label)与进度条(ProgressBar)从上下纵向堆叠改为同一行水平排列
- 具体：左列 `View` 的 `flexDirection` 从默认 `column` 改为 `row`，label 固定宽度 42px，ProgressBar 用 `flex: 1` 填充剩余空间
- 已重新 `expo export --platform web` 构建并启动预览服务器 (端口 8082)
