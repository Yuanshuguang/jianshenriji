# 2025-06-23 工作日志

## UI 重设计：5 套风格方案
- 用户要求对健身饮食动态计划助手 APP 进行 UI 风格重做
- 研究了 2024-2025 主流移动 APP 设计趋势（Whoop/Strava/Apple Fitness/Calm/Bento 等）
- 设计并渲染了 5 套完整方案：
  1. Athletic Brutalism (竞技粗野) - 黑底电光橙高对比
  2. Soft Gradient Wellness (柔和渐变康养) - 奶白暖橙圆角
  3. Editorial Minimal (编辑级极简) - 白底衬线靛蓝
  4. Bento Glass (便当玻璃) - 深空毛玻璃霓虹
  5. Organic Earth (有机大地) - 燕麦赤陶鼠尾草
- 输出：5 张手机高保真效果图 + 5 份设计哲学文档 + 1 张并排对比图
- 位置：ui-redesign/ 目录
- **等待用户挑选方向后继续产出 Design Tokens 和全页面稿**

## 用户选定方向：Bento Glass（第四款）
- 用户选择 Bento Glass · 便当玻璃 风格
- 产出完整设计系统：
  1. design-tokens.md - 设计规范文档（色彩/排版/间距/圆角/玻璃质感/动效/组件规范/无障碍）
  2. design-tokens.ts - 可直接用于 React Native 的代码 token（colors/typography/spacing/radius/bento/shadows/motion + glassTileStyle 工厂函数 + dataColor 语义映射）
  3. 11 个 MVP 页面高保真效果图：
     - Onboarding 4 页：body / body_shape / goal / training
     - 核心 5 页：today(首页) / nutrition_plan / nutrition_actual / training_plan / training_actual
     - 辅助 2 页：adjustment(动态调整) / history(历史趋势)
  4. 组件库展示页：色板/字体阶梯/玻璃格变体/进度组件/按钮/徽章/输入框/Tab Bar
  5. 全部页面并排总览图
- 位置：ui-redesign/bento-glass/ 目录
- 渲染框架：bg_common.py（共享玻璃质感/字体/配色/组件原语）
- 字体：Outfit(标题) + GeistMono(数字/标签) + 微软雅黑(中文)
- **下一步**：用户确认后可进入实际代码实现阶段，把 token 落到 shared/components 和 apps/mobile

## 代码落地：Bento 组件库 + 今日首页重构
- 用户选定 Bento Glass 后要求"继续推进"，进入代码实现阶段
- 在 shared/design-tokens.ts 落地纯 token（无 RN 依赖）：colors/glows/typography/spacing/radius/bento/motion/ringGradient/dataColor/semanticGlow，并在 shared/index.ts 导出
- 在 apps/mobile/components/bento/ 实现完整 RN 组件库（12 个文件）：
  - tokens.ts：从 shared 导入 + RN 样式工厂（glassTileStyle/textStyle/screenContainer）
  - Text.tsx：统一文本组件（variant/weight/color/mono/tracking/uppercase）
  - Label.tsx：全大写等宽小标签
  - GlassTile.tsx：毛玻璃卡片（raised/glow/radius/padding）
  - ProgressRing.tsx：渐变进度环（Animated + react-native-svg）
  - ProgressBar.tsx：胶囊进度条（超额自动变 warn 色）
  - Button.tsx：filled/glass/ghost 三种变体
  - Badge.tsx：玻璃底语义色徽章
  - Screen.tsx + ScreenHeader：页面容器 + 头部（kicker/title/subtitle/badge）
  - BentoGrid.tsx：BentoRow/BentoCol/BentoTile/BentoTriple 布局助手
  - TabBar.tsx：底部浮玻璃 TabBar（BentoTabBar）
  - MetricBlock.tsx：标签+大数字+单位 仪表盘单元
  - index.ts：统一出口
- 用 bento 组件重写 apps/mobile/app/(tabs)/_layout.tsx（BentoTabBar 替换旧 TabBar）
- 用 bento 组件重写 apps/mobile/app/(tabs)/index.tsx 为 Bento 风格今日首页：
  - 保留全部业务逻辑（食物解析/联网补充/餐次计划/训练消耗/动态调整）
  - UI 重构为 Bento 网格：热量环大格+消耗/赤字侧格 / 宏量三连格 / 训练横条 / 饮食记录卡 / 推荐餐次卡 / 动态调整卡
- typecheck 验证：
  - shared/design-tokens.ts 单独 npx tsc 验证通过（exit 0 无错）
  - mobile 完整 typecheck 依赖 pnpm install 完成网络安装（环境网络较慢，后台仍在跑）
- **架构决策**：纯 token 放 shared（跨端复用），RN 组件放 apps/mobile/components/bento（依赖 RN）。字体暂用 "System" 回退，后续装 Outfit/GeistMono 字体包后切换 fontFamilies。

## 迁移剩余 5 个页面到 Bento 风格
- 用户要求"继续迁移剩余页面"
- 新增 2 个 bento 表单组件：
  - LabeledInput.tsx：玻璃底输入框 + 上方小标签
  - SelectChip.tsx：可选中胶囊标签（支持 block 占满宽度）
- 迁移 (tabs)/train.tsx（训练 tab）：
  - Bento 布局：消耗进度环大格 + 部位侧格 / 训练计划横条 / 实际训练卡（状态切换+文本输入+分钟/疲劳输入）
  - 保留全部业务逻辑：状态切换、消耗估算、下一次训练预测、疲劳度
- 迁移 (tabs)/more.tsx（设置 tab）：
  - 我的菜单卡（拍照/识别营养表/自定义食物录入/列表删除）
  - 身体数据 / 目标 / 训练习惯 三个入口卡
  - 保留 Web 平台图片上传 + 移动端相机入口预留
- 迁移 onboarding 3 页：
  - body.tsx：性别选择 / 年龄+体重双列 / 身高 / 训练水平三选
  - goal.tsx：目标体重+周期双列 / 目标体型选择
  - training-preference.tsx：每周天数+每次分钟双列 / 有氧比例 / 可用器材多选 / 偏好部位多选
- 所有页面均使用 Screen + ScreenHeader + GlassTile + Button + SelectChip + LabeledInput 等 bento 组件
- shared/design-tokens.ts typecheck 验证通过（exit 0）
- **全部 8 个页面（含 _layout）已迁移完成，bento 组件库 14 个文件**

## 启动可交互 Web 预览（验牌）
- 用户要求"启动服务器验牌"
- Expo 依赖因 pnpm install 网络卡住未完成，无法直接跑 Expo
- 替代方案：做了零依赖的可交互 Web 预览（ui-redesign/preview/index.html）
  - 纯 HTML/CSS/JS，不依赖 RN/Expo，用 Node http.server 跑在 localhost:3456
  - 完整复刻 Bento Glass 风格：深空背景、毛玻璃格、霓虹辉光、SVG 渐变进度环
  - 3 个 tab 页面可切换：今日 / 训练 / 更多
  - 今日首页可交互：输入食物名称实时匹配食品库（11 种食物）、自动分配餐次、更新热量环/宏量进度条/动态调整卡
  - 训练页：状态切换 chip、训练动作列表
  - 设置页：我的菜单表单、身体数据/目标/训练习惯入口
- 启动命令：node http.server localhost:3456
- **说明**：这是设计验证预览，非最终 RN 代码运行。RN 代码已全部落地，等 pnpm install 完成后可跑真实 Expo。

## 首页仪表盘重构：核心三指标 + 可折叠详情
- 用户要求精简首页仪表盘为核心三卡片 + 可折叠详情
- 新增 CollapsibleCard.tsx 组件（Animated 高度/透明度过渡 + 箭头旋转）
- Web 预览 (index.html) 重构：
  - 核心三卡片（BentoTriple）：今日摄入 / 今日消耗 / 赤字
  - 可折叠详情卡（查看详情按钮）：热量环 + 宏量三连 + 周趋势柱状图 + 营养配比分析条
  - 输入食物后实时更新核心三卡片和折叠卡内全部数据
  - 营养配比智能提示（根据蛋白/脂肪/碳水占比给出不同建议）
- RN 代码 (apps/mobile/app/(tabs)/index.tsx) 同步重构：
  - 核心三卡片替代原"热量大格+侧格+宏量三连"
  - CollapsibleCard 包裹热量环 + 宏量 + 营养配比分析
  - 保留全部业务逻辑
