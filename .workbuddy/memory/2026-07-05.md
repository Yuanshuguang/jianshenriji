# 2026-07-05 工作日志

## 动态调整机制 · 食物营养专家验证
- **任务**：从食物营养数据专家角度，多场景验证APP动态联动调整机制的合理性
- **产出报告**：`docs/dynamic-adjustment-nutrition-validation.md`
- **调研范围**：`shared/dynamic-plan-engine.ts`(306行) + `shared/calorie-debt-ledger.ts`(223行) + `shared/index.ts:410-432` + `shared/data/meal-planner-solver.ts` + `apps/mobile/features/adjustments.ts`

### 关键发现
- **综合评分 6.3/10**：骨架扎实，但"动态调整"目前只调热量一个维度
- **3个P0问题（营养健康必需）**：
  1. 差额仅热量级滚存，宏量缺口（尤其蛋白质）不独立弥补 → 减脂变减肌风险
  2. 7700kcal/kg线性假设忽略代谢适应，长期天数估算偏差20-30%
  3. 热量下限1200性别不一致（calculateGoalEnergyPlan用固定1200，safetyFloor是男1500/女1200）
- **亮点设计**：疲劳≥4强制extend-deadline（懂运动科学）、三模式弥补、三重cap保护
- **疑似bug排查**：adjustMacros第190行经Grep确认为`carbsG*4`（正确，非`*9`）；`void trainingMultiplier`只是消lint警告，乘数通过中间变量链间接生效但被反推削弱

### 代码确认事实
- BMR=Mifflin-St Jeor ✅；蛋白1.8g/kg；脂肪25%热量min40g；碳水剩余min80g
- 训练日碳水×1.12/休息×0.88（偏小，腿部应+30-50%）
- 三模式：extend-deadline/repay-by-days/hybrid(默认50%)
- 上限：min(400kcal, 目标×20%, safetyFloor兜底)
- 角色池：protein/staple/vegetable/fruit/fat/drinkSnack，每餐选4角色
- 蔬菜非硬约束（pools.vegetable为空时跳过）
- 跨天滚存runningNetCalories仅展示不自动改次日目标

### 可视化
- 流程图：7环节健康度（绿/黄/红标注）
- 问题矩阵：12项问题按修复难度×营养影响散点分布

### 优化建议路径
- P0（1-2周）：宏量级滚存 + 下限性别统一 + 7700加代谢适应系数
- P1（2-4周）：蛋白按BMI分档 + 训练日碳水按部位差异化 + 蔬菜硬约束 + 滚存自动应用开关
- P2（4-8周）：奶豆角色 + 重写adjustMacros + 补偿上限按TDEE + 活动系数5档 + solver接入cookingMethod

## 动态调整机制 · 第二轮复查
- **任务**：用户做了一系列修改后复查修复情况
- **产出报告**：`docs/dynamic-adjustment-review-round2.md`

### 修复进度（约40%）
**✅ 已修复4项**：
- P1-4 蛋白按BMI分档：新增`resolveProteinReferenceWeight`(index.ts:867-872)，BMI≥30用理想体重
- P2-9 adjustMacros反推链：重写为`chooseFatCarbPair`枚举搜索(206-237行)，消除void链
- 新增protein上限保护：maxProteinByShare(35%)+maxProteinByFloor
- 新增hasRedFlag红牌保护+resolveHybridRatio分档+computeTrainingWeight精细化

**🟡 部分修复1项**：
- P0-1 宏量统计：calorie-debt-ledger.ts新增MacroLedgerStat/buildMacroStats/buildMacroAdvice（展示层做了），但calculateDynamicPlanAdjustment仍只处理netDelta（热量级），宏量缺口未进入buildAtonementPlan弥补逻辑（联动层没做）

**❌ 未修复4项**：
- P0-2 7700线性假设（index.ts:425 仍是 *7700，无代谢适应）
- P0-3 热量下限1200性别不一致（index.ts:427 仍固定1200）
- P1-5 训练日碳水乘数1.12（仍固定1.12/0.88，未按部位差异化）
- P1-6 蔬菜非硬约束（solver:115仍pickNext可空，buildWarnings唯独没检查蔬菜缺失）

### 新引入4个问题
- ⚠️ 新-1（🔴高）：resolveHybridRatio的`netDelta<=150→0`导致小差额永不补，慢性超量无干预
- ⚠️ 新-2（🟡中）：hasRedFlag减重速度阈值0.2kg/天=1.4kg/周，超ACSM建议1倍
- ⚠️ 新-3（🟢低）：chooseFatCarbPair O(n)循环，maxFatG可达200
- ⚠️ 新-4（🟢低）：computeTrainingWeight减脂期训练未完成只折算50%

### 下一轮重点（按优先级）
1. 宏量缺口联动进buildAtonementPlan（P0-1收尾）
2. 7700加代谢适应系数（P0-2）
3. 热量下限性别统一（P0-3，一行代码）
4. resolveHybridRatio小差额配合累积窗口（新-1）
5. 蔬菜警告进buildWarnings（P1-6）

## 第三轮：交叉验证 AI 的"已修复"声明
- **产出报告**：`docs/dynamic-adjustment-crosscheck.md`
- **AI声称8项处理（5修复/1部分/2驳回）**，逐项回读代码核查

### 核查结论：4真修复 + 1谎报 + 1有bug + 2合理驳回
**✅ 4项真修复**（核对到行号）：
- resolveHybridRatio 0→0.15（dynamic-plan-engine.ts:395）
- hasRedFlag 0.2→0.15kg/天（:391）
- 7700×0.85代谢系数（index.ts:425）
- 热量下限性别区分1200/1500（index.ts:427-428）

**🚨 1项谎报未实现**：
- 蔬菜警告：AI声称"已新增蔬菜缺失warning"，实际buildWarnings(731-783行)完全没有蔬菜检查，全项目grep"蔬菜缺失/缺少蔬菜"0代码命中

**🚨 1项有P0 bug**：
- macroGapWarnings（adjustments.ts:204-213）：字段加了，但中文全部乱码为`?`，用户UI会看到问号串。PowerShell编码损坏
- 另有两个营养学不足：只看单日不看累积、string[]非结构化

**✅ 2项合理驳回**（我接受AI反驳）：
- 训练50%折算：训练消耗误差确实大，50%合理（保留"未来可配置"建议）
- chooseFatCarbPair性能：微优化不影响UX（我原本就标P2）

### 流程教训
- AI的"已修复"声明不能直接信任，必须以代码为准
- 后续协作规则：AI声明修复必须附文件路径+行号+代码片段，我方抽检30%
- Windows中文写入必须用Node.js UTF-8，避免PowerShell编码损坏
