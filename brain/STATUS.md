# STATUS - 当前状态
> 这个文件回答：现在做到哪了、下一步做什么、卡在哪里。

## 我们现在在做什么
当前重点是把饮食和训练主链路继续收口，尤其是：
1. 饮食计划和训练计划的联动闭环
2. 今日餐次、储备食物、实际饮食三者的边界
3. AI 识别结果默认写入实际饮食，需要时再手动加入储备食物库
4. 动态调整“赎罪”机制、预警和总量闭合展示
5. 动作库操作菜单改成按钮下拉式浮层，并清理不再需要的“讲解”入口

## 当前进度
- 当前工作树里已经有一批移动端 UI、共享组件、饮食逻辑和动作库的未提交改动。
- 动态调整已经新增“赎罪”机制：温和赎罪、限时赎罪、顺延目标三种方式已接入共享规则层和首页 UI。
- 餐次偏差基线已改为当前饮食方案真实餐次目标；营养子开关已接入宏量调整。
- 最近一次验证结果是：
  - `pnpm --dir apps/mobile run typecheck`
  - `pnpm --dir apps/mobile test`
  - `pnpm --dir shared exec tsx --test dynamic-plan-engine.test.ts`
  这三项都已通过。

## 下一步
1. 继续做动态调整的可视化验收，确认首页“赎罪”卡片在移动端布局里不拥挤。
2. 清理中文乱码和视觉残留问题。
3. 如果后续确认没有功能缺口，就把当前分支整理成可提交状态。

## 阻塞 / 需要确认
- 目前没有新的外部阻塞。

## 最近一次会话做了什么
- 建立了项目脑 `brain/`，并用中文模板补齐项目定义、地图、状态、决策和交接。
- 修复了 AI 菜品识别默认写入真实饮食的问题，并补充“加入储备食物库”的二次操作。
- 在自定义动态调整弹窗顶部加入了“调整后合计 vs 今日目标”的预警栏。
- 把动作库页的操作菜单改成按钮下拉式浮层，移除了原来的上方大卡片结构。
- 新增动态调整“赎罪”机制，并接入前端首页和共享规则层。
- 当前代码验证已经通过 `pnpm --dir apps/mobile run typecheck`、`pnpm --dir apps/mobile test`、`pnpm --dir shared exec tsx --test dynamic-plan-engine.test.ts`。
