# MVP 开发路线

## 1. 当前阶段

当前仓库已有：

- `C:\Users\Administrator\Documents\健身日历\PRD.md`
- `C:\Users\Administrator\Documents\健身日历\TECHNICAL_PLAN.md`
- `C:\Users\Administrator\Documents\健身日历\apps\mobile`
- `C:\Users\Administrator\Documents\健身日历\apps\api`
- `C:\Users\Administrator\Documents\健身日历\shared`

本路线已调整为离线优先：MVP 不依赖 AI、不依赖联网。

## 2. 开发原则

- 先做离线动态计划闭环，再做联网增强。
- 先做本地数据库和公式引擎，不接真实 AI。
- 先做选择式/表单式输入，再做自然语言和语音输入。
- 先做手动补充，再考虑穿戴设备同步。
- 食品库和动作库先覆盖高频数据，不做大而全。
- 所有公共组件放 `shared/components`。
- 所有公共数据放 `shared/data`。
- 所有核心公式放 `shared/domain`。
- 不删除已有功能，不随意修改其他模块。

## 3. 阶段一：工程初始化

状态：已基本完成。

已完成：

1. 初始化 Expo + TypeScript 移动端。
2. 初始化 API 服务。
3. 创建 `shared/components`、`shared/data`、`shared/domain`、`shared/ai`、`shared/types`。
4. 配置 pnpm workspace。
5. 创建基础首页和 API 健康检查。

验收：

- `pnpm typecheck` 通过。
- API `/api/health` 可返回健康状态。

## 4. 阶段二：本地数据和公式引擎

目标：

- 不依赖 AI，不依赖 API，先跑通核心数字计算。

建议任务：

1. 扩展基础食品库。
2. 扩展基础动作库。
3. 实现 BMR / TDEE 计算。
4. 实现宏量营养目标计算。
5. 实现食品匹配。
6. 实现食物克数分配。
7. 实现实际摄入热量计算。
8. 实现热量偏差分摊。
9. 实现训练消耗估算。
10. 实现训练队列生成、顺延和重排。

验收：

- 给定用户身体数据和目标，能算出每日热量目标。
- 给定食物列表，能分配推荐克数。
- 给定实际摄入，能计算热量和营养偏差。
- 给定超额摄入，能分摊到未来 3 到 7 天。
- 给定训练动作和时长，能估算消耗。
- 给定未完成训练，能顺延训练队列。

## 5. 阶段三：移动端离线核心流程

目标：

- 跑通完整离线 MVP 使用路径。

建议任务：

1. 初始身体数据页面。
2. 目标设置页面。
3. 体态图选择页面。
4. 训练习惯页面。
5. 今日推荐首页。
6. 饮食选择页面。
7. 实际饮食补充页面。
8. 训练推荐页面。
9. 实际训练补充页面。
10. 动态调整说明页面。
11. 历史趋势页面。

验收：

- 用户能完成初始设置。
- 用户能离线生成当天饮食和训练推荐。
- 用户能补充实际饮食和训练。
- 系统能展示调整结果和解释。

## 6. 阶段四：本地持久化

目标：

- 让 App 关闭后仍能保留用户数据和历史计划。

建议任务：

1. 先用 Zustand persist 或 AsyncStorage 保存基础状态。
2. 设计本地数据版本号。
3. 保存用户身体档案、目标、训练习惯。
4. 保存每日饮食记录。
5. 保存每日训练记录。
6. 保存动态调整结果。

验收：

- App 重启后能恢复用户档案和历史记录。
- 本地数据升级有版本兼容策略。

## 7. 阶段五：离线输入体验优化

目标：

- 降低用户每日输入成本，但仍不依赖 AI。

建议任务：

1. 食品搜索和别名匹配。
2. 最近常吃食物。
3. 常用组合。
4. 动作搜索和别名匹配。
5. 最近训练动作。
6. 快速复制昨日计划。

验收：

- 用户可以快速选择常见食物和动作。
- 用户不需要每次从零输入完整内容。

## 8. 阶段六：未来增强预留

目标：

- 只预留接口和数据结构，不影响离线主流程。

可选方向：

- AI 自然语言解析。
- 语音转文字。
- 拍照估算体脂。
- Apple Health / Google Fit。
- 云端备份和多设备同步。

验收：

- 关闭所有联网能力时，核心功能仍可正常使用。
- 未来接入增强能力时不需要重构计划引擎。

## 9. 优先级排序

P0：

- 身体和目标设置
- 食品库
- 动作库
- 饮食推荐克数
- 实际饮食补充
- 热量偏差重排
- 训练习惯设置
- 训练推荐
- 实际训练补充
- 训练队列重排
- 本地持久化

P1：

- 食品搜索和别名匹配
- 动作搜索和别名匹配
- 动态调整解释
- 历史趋势
- 体态图选择

P2：

- 自定义食物
- 自定义动作
- 常用组合
- 最近记录快捷输入

P3：

- AI 自然语言解析
- 体脂照片估算
- 语音输入
- 可穿戴设备同步
- 云端备份

## 10. 下一步建议

下一步进入阶段二：

1. 扩展 `shared/data/foods.ts`。
2. 扩展 `shared/data/exercises.ts`。
3. 补 `shared/domain/nutrition`。
4. 补 `shared/domain/training`。
5. 补热量偏差分摊函数。
6. 用 `pnpm typecheck` 验证离线公式模块。
